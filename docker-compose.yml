services:
  dauth-service:
    image: sivarajan001/dauth-service:latest
    container_name: dauth-service

    restart: unless-stopped

    ports:
      - "8100:8000"

    env_file:
      - ./dauth_service/.env

    volumes:
      - ./dauth_service/logs:/app/logs

